{
  "hooks": {
    "preToolUse": [
      {
        "command": "command -v stele >/dev/null 2>&1 || exit 0; stele hook cursor pre-tool-use --scope repo",
        "timeout": 30
      }
    ],
    "sessionStart": [
      {
        "command": "command -v stele >/dev/null 2>&1 || exit 0; stele hook cursor session-start --scope repo",
        "timeout": 30
      }
    ],
    "stop": [
      {
        "command": "command -v stele >/dev/null 2>&1 || exit 0; stele hook cursor stop --scope repo",
        "timeout": 30
      }
    ]
  },
  "version": 1
}
